# Sample Codex configuration
# Used by crates/core/src/control/apply.rs tests: apply must preserve unrelated tables
# and comments, and manage only [mcp_servers.mindctx].

[model]
model = "gpt-5"

[mcp_servers.mindctx]
command = "/usr/local/bin/mindctx"
args = ["serve"]

[mcp_servers.mindctx.env]
# MINDCTX_TOKEN_BUDGET = "4000"
