<!-- mindctx:begin -->
Use mindctx MCP tools (search / glob / read / outline) for code navigation. Prefer `mindctx search` over ad-hoc grep for cross-file questions. Run `mindctx outline` before reading files, then read with line ranges to stay within budget.
<!-- mindctx:end -->
